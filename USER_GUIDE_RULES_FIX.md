# Инструкция по использованию исправлений

**⚠️ КРИТИЧЕСКОЕ ОБНОВЛЕНИЕ от 25.11.2025: Исправлен DNS и TUN**

## Что исправлено

### 🔥 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ DNS и TUN

**Проблема:** РФ-режим и исключения не работали из-за конфликта DNS fake-ip с DIRECT-маршрутизацией.

**Решение:**
- ✅ DNS fake-ip-filter теперь исключает .ru/.su/.рф из fake-ip пула в РФ-режиме
- ✅ DNS nameserver-policy использует российские DNS (Yandex 77.88.8.8) для российских доменов
- ✅ TUN dns-hijack перехватывает **все** DNS-запросы (порт 53 UDP/TCP)
- ✅ TUN strict-route обеспечивает строгую маршрутизацию без обходов

**Теперь работает как ожидается!**

### 1. Режим РФ теперь РАБОТАЕТ правильно ✅

**Как работает:**
- **РФ-режим ВКЛЮЧЕН:** российский трафик (.ru/.su/.рф + GEOIP RU) идёт НАПРЯМУЮ (в обход VPN)
- **РФ-режим ВЫКЛЮЧЕН (глобальный):** весь трафик через VPN, кроме исключений и локальных сетей

**Проверка:**
1. Включите РФ-режим (переключатель "Режим РФ")
2. Запустите VPN
3. Откройте https://2ip.ru — должен показать ваш РЕАЛЬНЫЙ российский IP (не IP туннеля)
4. Откройте https://google.com — должен показать IP туннеля (другая страна)

### 2. Исключения теперь РАБОТАЮТ ✅

**Как работает:**
- Домены/приложения из исключений ВСЕГДА идут в обход VPN
- Исключения имеют **ВЫСШИЙ ПРИОРИТЕТ** (выше РФ-режима)

**Пример:**
- Добавили `yandex.ru` в исключения
- Включили РФ-режим
- Результат: `yandex.ru` идёт DIRECT (в обход VPN), хотя это .ru домен

**Проверка:**
1. Добавьте `google.com` в исключения (Настройки → Исключения → Добавить домен)
2. Запустите VPN в ГЛОБАЛЬНОМ режиме
3. Откройте https://google.com — должен показать ваш реальный IP (не IP туннеля)

### 3. Изменения применяются автоматически ✅

**Как работает:**
- При изменении режима РФ/GLOBAL туннель автоматически перезапускается
- При добавлении/удалении исключений туннель автоматически перезапускается
- В логах появляется: "Перезапуск туннеля для применения изменений..."

**Проверка:**
1. Запустите VPN
2. Переключите режим РФ ↔ GLOBAL
3. В логах появится сообщение о перезапуске
4. Через ~1-2 секунды туннель перезапустится с новыми настройками

### 4. Логи всегда работают ✅

**Что изменилось:**
- Убрана кнопка play/pause (логи **ВСЕГДА** стримятся)
- Добавлен умный автоскроллинг:
  - 🟢 **Авто** (зелёный индикатор) — новые логи автоматически появляются внизу
  - ⚫ **Стоп** (серый индикатор) — вы читаете старые логи, автоскролл выключен
- Кнопка "Вниз" (📍) — быстрый переход к последним логам

**Как пользоваться:**
1. Откройте вкладку "Логи"
2. Логи автоматически прокручиваются вниз (индикатор "Авто" 🟢)
3. Прокрутите вверх, чтобы прочитать старые логи → индикатор сменится на "Стоп" ⚫
4. Нажмите кнопку "Вниз" (📍) или прокрутите в самый низ → автоскролл включится обратно

## Часто задаваемые вопросы

### Q: Как понять, что РФ-режим работает?
**A:** Откройте https://2ip.ru — должен показать ваш реальный российский IP. Если показывает IP другой страны — режим РФ выключен (или не работает).

### Q: Как добавить исключение для приложения?
**A:** 
1. Откройте Настройки → Исключения
2. Нажмите "Добавить приложение"
3. Введите имя процесса, например: `chrome.exe`, `telegram.exe`, `steam.exe`
4. Нажмите "Добавить"
5. Туннель автоматически перезапустится

### Q: Как добавить исключение для сайта?
**A:**
1. Откройте Настройки → Исключения
2. Нажмите "Добавить домен"
3. Введите домен, например: `google.com`, `youtube.com`, `twitch.tv`
4. Нажмите "Добавить"
5. Туннель автоматически перезапустится

### Q: Почему логи перестали прокручиваться вниз?
**A:** Вы прокрутили вверх вручную — автоскролл автоматически выключился. Нажмите кнопку "Вниз" (📍) или прокрутите в самый низ, чтобы включить автоскролл обратно.

### Q: Как проверить, что исключения работают?
**A:** Откройте `config_debug.yaml` в папке с приложением. В секции `rules:` исключения должны быть **ПЕРВЫМИ** (перед `GEOIP,private` и РФ-правилами).

### Q: Что делать, если РФ-режим не работает?
**A:**
1. Проверьте, что переключатель "Режим РФ" ВКЛЮЧЕН
2. Перезапустите туннель
3. Откройте `config_debug.yaml` и убедитесь, что есть правила:
   ```yaml
   - DOMAIN-SUFFIX,ru,DIRECT
   - DOMAIN-SUFFIX,su,DIRECT
   - DOMAIN-SUFFIX,рф,DIRECT
   - GEOIP,RU,DIRECT,no-resolve
   ```
4. Проверьте логи на наличие ошибок

## Отладка

### Проверка порядка правил
Откройте файл `config_debug.yaml` в папке с приложением. Правила должны быть в таком порядке:

```yaml
rules:
  # 1. Исключения по приложениям (если есть)
  - PROCESS-NAME,chrome.exe,DIRECT
  
  # 2. Исключения по доменам (если есть)
  - DOMAIN-SUFFIX,google.com,DIRECT
  
  # 3. Локальные сети
  - GEOIP,private,DIRECT,no-resolve
  
  # 4. РФ-режим (если включен)
  - DOMAIN-SUFFIX,ru,DIRECT
  - DOMAIN-SUFFIX,su,DIRECT
  - DOMAIN-SUFFIX,рф,DIRECT
  - GEOIP,RU,DIRECT,no-resolve
  - DOMAIN-SUFFIX,2ip.ru,DIRECT
  
  # 5. Всё остальное через VPN
  - MATCH,VLF
```

Если порядок другой — напишите в Issues на GitHub.

## Известные ограничения

1. **Буфер логов:** хранится максимум 5000 строк (старые автоматически удаляются)
2. **Автоматический перезапуск:** занимает ~1-2 секунды, во время перезапуска туннель недоступен
3. **Исключения по приложениям:** работают только для процессов Windows (указывается имя .exe файла)

## Обратная связь

Если что-то не работает:
1. Откройте вкладку "Логи" и скопируйте последние 50-100 строк
2. Откройте файл `config_debug.yaml` и скопируйте секцию `rules:`
3. Создайте Issue на GitHub с описанием проблемы и логами
