# VLF Tunnel - Реализованные улучшения

## Дата: 26 ноября 2025

### ✅ Task 1: System Tray Support (Windows)

**Что сделано:**
- Добавлена зависимость `tray_manager: ^0.2.3` для поддержки системного трея
- Создан `TrayHandler` класс для управления иконкой трея и меню
- Перехват события закрытия окна: нажатие на X → скрытие в трей (а не выход из приложения)
- Туннель продолжает работать в фоне даже при скрытом окне
- Контекстное меню трея:
  - "Открыть VLF tunnel" — восстановление окна
  - "Отключиться и выйти" — корректная остановка туннеля + выход из приложения
- Иконка трея скопирована из `windows/runner/resources/app_icon.ico`

**Файлы:**
- `lib/tray_handler.dart` — обработчик трея и событий окна
- `lib/main.dart` — интеграция TrayHandler в Bootstrap
- `pubspec.yaml` — добавлена зависимость tray_manager
- `assets/tray_icon.ico` — иконка для трея

**Важно:** Гарантируется корректная остановка mihomo.exe перед выходом (нет orphan процессов)

---

### ✅ Task 2: PROXY Mode Implementation

**Что сделано:**

#### 2.1 Enum для режимов работы
- Создан `VlfWorkMode` enum с двумя значениями: `tun` и `proxy`
- Добавлены extension методы `displayName` и `description` для локализации

**Файлы:** `lib/core/vlf_work_mode.dart`

#### 2.2 Генерация конфигурации для PROXY режима
- Добавлена функция `buildClashConfigProxy()` в `clash_config.dart`
- **Отличия от TUN режима:**
  - НЕТ секции `tun` (не требует wintun.dll, не нужны права администратора)
  - `mixed-port: 7890` — HTTP + SOCKS на одном порту
  - `socks-port: 7891` — дополнительный SOCKS порт
- **Общие части:**
  - Правила маршрутизации ИДЕНТИЧНЫ TUN режиму (RF-mode, исключения работают одинаково)
  - DNS конфигурация идентична
  - Proxy и proxy-groups идентичны

**Файлы:** `lib/clash_config.dart`

#### 2.3 Интеграция в VlfCore и ClashManager
- Добавлен `ValueNotifier<VlfWorkMode> workMode` в `VlfCore`
- Метод `setWorkMode(VlfWorkMode)` — переключение режима с автоматическим рестартом туннеля
- `ClashManager.start()` принимает параметр `workMode` и генерирует соответствующий config.yaml
- Режим сохраняется в `vlf_gui_config.json` (поле `work_mode`: `'tun'` или `'proxy'`)
- `_generateConfigFiles()` автоматически выбирает правильный билдер конфига

**Файлы:**
- `lib/core/vlf_core.dart`
- `lib/clash_manager.dart`

#### 2.4 UI для переключения режимов
- Создан виджет `WorkModeSwitch` — segmented control с двумя кнопками (TUNNEL / PROXY)
- Создан виджет `ProxyModeInfo` — информационная панель с адресами прокси
- Интегрировано в `HomeScreen`:
  - Переключатель под кнопкой RU-режима
  - При переключении показывается снэкбар с уведомлением
  - В PROXY режиме отображается инфо-панель с `127.0.0.1:7890` и `127.0.0.1:7891`

**Файлы:**
- `lib/ui/widgets/work_mode_switch.dart` — виджеты WorkModeSwitch и ProxyModeInfo
- `lib/ui/home_screen.dart` — интеграция в UI

#### 2.5 Тесты
- Создан `test/clash_config_proxy_test.dart` с 6 тестами:
  - Проверка отсутствия TUN секции в PROXY конфиге
  - Идентичность правил между TUN и PROXY режимами
  - Корректная работа RF-режима в PROXY
  - Обработка исключений (домены, приложения)
  - Корректные значения портов
- ✅ Все тесты проходят успешно

**Файлы:**
- `test/clash_config_proxy_test.dart`
- Удален устаревший `test/widget_test.dart` (шаблонный)

---

### 🔄 Task 3: Core/Wrapper Architecture (НЕ РЕАЛИЗОВАНО)

**Причина:**
- Task 3 — большая архитектурная работа по выделению platform-independent кода в отдельный пакет
- Требует рефакторинга существующей кодовой базы
- Лучше реализовывать отдельным PR после тестирования Task 1 и Task 2

**Что нужно сделать (для будущего):**
1. Создать `packages/vlf_core` — pure Dart пакет
2. Определить интерфейс `PlatformRunner` для абстракции запуска mihomo
3. Переместить profile management, subscription parsing, config generation в vlf_core
4. Реализовать `WindowsPlatformRunner` в основном приложении
5. Для Android/iOS/macOS/Linux реализовать свои PlatformRunner

---

## Гарантии стабильности TUN режима

✅ **TUN режим НЕ СЛОМАН:**
- Все существующие тесты проходят (`test/clash_config_rules_test.dart`)
- Правила маршрутизации идентичны (RF-mode, исключения, порядок правил)
- `buildClashConfig()` не изменена (только добавлена `buildClashConfigProxy()`)
- ClashManager по умолчанию работает в TUN режиме (backward compatibility)
- Конфигурационный формат расширен (добавлено поле `work_mode`), но остается совместимым

---

## Файловая структура изменений

### Новые файлы:
```
lib/
  core/
    vlf_work_mode.dart           # Enum для режимов работы
  ui/
    widgets/
      work_mode_switch.dart      # UI компоненты для переключения режимов
  tray_handler.dart              # Обработчик системного трея
assets/
  tray_icon.ico                  # Иконка для трея
test/
  clash_config_proxy_test.dart   # Тесты для PROXY режима
```

### Изменённые файлы:
```
pubspec.yaml                     # + tray_manager, + assets
lib/main.dart                    # + tray initialization, TrayHandler integration
lib/clash_config.dart            # + buildClashConfigProxy()
lib/clash_manager.dart           # + workMode parameter
lib/core/vlf_core.dart           # + workMode, setWorkMode(), mode persistence
lib/ui/home_screen.dart          # + WorkModeSwitch, ProxyModeInfo
```

---

## Как использовать

### System Tray:
1. Запустите приложение
2. Нажмите X на окне — приложение скроется в трей (туннель продолжит работать)
3. Кликните на иконку трея → откроется окно
4. ПКМ на иконке трея → "Отключиться и выйти" → корректный выход

### PROXY Mode:
1. На главном экране под кнопкой RU-режима находится переключатель TUNNEL / PROXY
2. Выберите PROXY → туннель перезапустится в режиме прокси
3. Появится информационная панель с адресами:
   - HTTP/SOCKS: `127.0.0.1:7890`
   - SOCKS только: `127.0.0.1:7891`
4. Настройте приложения на использование этих прокси вручную
5. RF-режим и исключения работают так же, как в TUN режиме

---

## Тестирование

```powershell
# Запуск всех тестов
flutter test

# Результат:
# 00:01 +10: All tests passed!
```

Тесты покрывают:
- Правила маршрутизации для TUN и PROXY (идентичность)
- RF-режим в обоих режимах
- Исключения по доменам и приложениям
- Корректность портов в PROXY конфиге
- Отсутствие TUN секции в PROXY конфиге

---

## Что дальше?

### Краткосрочно (для релиза):
1. ✅ Протестировать трей на Windows (скрытие, восстановление, выход)
2. ✅ Протестировать PROXY режим (запуск, переключение, правила)
3. ✅ Протестировать TUN режим (убедиться что ничего не сломалось)
4. Собрать релизную версию с новыми фичами
5. Обновить README с описанием новых возможностей

### Среднесрочно (следующий PR):
1. Task 3: Core/Wrapper Architecture
2. macOS/Linux поддержка трея (tray_manager уже кроссплатформенный)
3. Android/iOS версии (через vlf_core пакет)
4. Автоматическая настройка системного прокси в PROXY режиме (опционально)

---

## Известные ограничения

### System Tray:
- Реализовано только для Windows (но tray_manager поддерживает macOS/Linux)
- При двойном клике на иконке — только восстановление окна (не toggle подключения)

### PROXY Mode:
- Требует ручной настройки прокси в приложениях
- Не работает прозрачное проксирование (в отличие от TUN)
- Правила по процессам (PROCESS-NAME) применяются Clash Meta, но эффект ограничен

---

*Автор: AI Agent (GitHub Copilot)*  
*Дата: 26.11.2025*
