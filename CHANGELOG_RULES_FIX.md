# Changelog: Исправления приоритета правил и UI логов

## Дата: 25 ноября 2025 г.

## Изменения

### 1. Исправлен порядок приоритета правил маршрутизации (config.yaml)

**Проблема:**
- Исключения (сайты и приложения) не работали, так как добавлялись ПОСЛЕ локальных сетей и РФ-правил
- Clash Meta проверяет правила сверху вниз, первое совпадение побеждает
- Из-за неправильного порядка исключения игнорировались

**Решение:**
Изменён порядок генерации правил в `lib/clash_config.dart`:

```yaml
rules:
  # 1. PROCESS-NAME (исключения по приложениям) — ВЫСШИЙ ПРИОРИТЕТ
  - PROCESS-NAME,chrome.exe,DIRECT
  - PROCESS-NAME,firefox.exe,DIRECT
  
  # 2. DOMAIN-SUFFIX (исключения по доменам) — ВЫСШИЙ ПРИОРИТЕТ
  - DOMAIN-SUFFIX,google.com,DIRECT
  - DOMAIN-SUFFIX,youtube.com,DIRECT
  
  # 3. GEOIP,private (локальные сети)
  - GEOIP,private,DIRECT,no-resolve
  
  # 4. РФ-режим (если включен)
  - DOMAIN-SUFFIX,ru,DIRECT
  - DOMAIN-SUFFIX,su,DIRECT
  - DOMAIN-SUFFIX,рф,DIRECT
  - GEOIP,RU,DIRECT,no-resolve
  - DOMAIN-SUFFIX,2ip.ru,DIRECT
  
  # 5. MATCH,VLF (всё остальное через VPN)
  - MATCH,VLF
```

**Преимущества:**
- ✅ Исключения теперь ВСЕГДА работают, независимо от режима
- ✅ Можно исключить даже .ru домены из РФ-режима
- ✅ Приложения из исключений идут DIRECT, даже если их трафик .ru

### 2. Добавлен автоматический перезапуск при изменениях конфигурации

**Проблема:**
- При изменении режима РФ/GLOBAL или добавлении/удалении исключений изменения не применялись
- Нужно было вручную останавливать и запускать туннель

**Решение:**
Добавлен метод `_restartIfRunning()` в `lib/core/vlf_core.dart`, который автоматически:
1. Останавливает туннель
2. Ждёт 500ms
3. Запускает туннель с новой конфигурацией

Метод вызывается при:
- Переключении режима РФ (`setRuMode()`)
- Добавлении исключения по домену (`addSiteExclusion()`)
- Удалении исключения по домену (`removeSiteExclusion()`)
- Добавлении исключения по приложению (`addAppExclusion()`)
- Удалении исключения по приложению (`removeAppExclusion()`)

**Преимущества:**
- ✅ Изменения применяются мгновенно
- ✅ Не нужно вручную перезапускать туннель
- ✅ В логах появляется сообщение о перезапуске

### 3. Переработан виджет логов

**Проблемы:**
- Кнопка play/pause останавливала поток логов (неудобно для отладки)
- Нет нормального автоскроллинга
- Нет видимого скроллбара

**Решение:**
Полностью переработан `lib/ui/widgets/log_panel.dart`:

#### Удалена механика play/pause
- Логи теперь **ВСЕГДА** стримятся, пока работает mihomo
- Нет кнопок паузы — непрерывный поток

#### Добавлен умный автоскроллинг
- По умолчанию автоскролл **включен** (новые логи автоматически показываются внизу)
- При ручной прокрутке вверх автоскролл **отключается** (можно читать старые логи)
- При прокрутке вниз (или нажатии кнопки "Вниз") автоскролл **включается** обратно

#### Новый UI
- Индикатор состояния автоскролла:
  - 🟢 **Авто** (зелёный) — автоскролл включен
  - ⚫ **Стоп** (серый) — автоскролл выключен (читаете старые логи)
- Кнопка "Вниз" (появляется когда автоскролл выключен) — быстрый переход к последним логам
- Видимый Scrollbar для удобной навигации
- Буфер логов: 5000 строк (старые автоматически удаляются)

**Преимущества:**
- ✅ Логи всегда доступны для мониторинга
- ✅ Удобно читать историю без дёрганья
- ✅ Быстрый возврат к последним логам
- ✅ Визуальная индикация состояния

## Тестирование

Создан тестовый скрипт `test_rules_priority.dart`, который проверяет:
1. Режим РФ без исключений
2. Режим РФ с исключениями (приложения и домены)
3. Глобальный режим с исключениями

Все тесты пройдены ✅

## Как проверить изменения

### 1. Порядок правил
```bash
# Запустить тест
dart run test_rules_priority.dart

# Или проверить вручную: запустить приложение и открыть config_debug.yaml
# Порядок правил должен быть: исключения → локальные сети → РФ-режим → MATCH
```

### 2. Автоматический перезапуск
1. Запустить VPN
2. Добавить исключение (домен или приложение) через UI
3. В логах появится: "Перезапуск туннеля для применения изменений..."
4. Туннель автоматически перезапустится
5. В логах появится: "Туннель перезапущен с новой конфигурацией"

### 3. Новый UI логов
1. Запустить VPN
2. Открыть вкладку "Логи"
3. Логи стримятся автоматически, внизу появляются новые строки
4. Прокрутить вверх — индикатор сменится на "Стоп" (серый)
5. Появится кнопка "Вниз"
6. Нажать "Вниз" или прокрутить в самый низ — индикатор сменится на "Авто" (зелёный)

## Файлы изменены

- `lib/clash_config.dart` — исправлен порядок генерации правил
- `lib/core/vlf_core.dart` — добавлен `_restartIfRunning()` и вызовы в методы изменения конфигурации
- `lib/ui/widgets/log_panel.dart` — полная переработка UI логов
- `test_rules_priority.dart` — новый тестовый скрипт

## Обратная совместимость

✅ Все изменения обратно совместимы:
- Старые конфиги (`vlf_gui_config.json`, `profiles.json`) работают без изменений
- TUN-режим и Clash/mihomo работают стабильно
- Существующие профили не требуют переконфигурации
